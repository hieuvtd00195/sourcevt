export interface AddExpire {
  showQuantity: number | null;
  code: string;
  storeName: string;
  phone: string;
  email: string;
  address: string;
  city: number | null;
  district: number | null;
  ward: number | null;
  size: string;
}
